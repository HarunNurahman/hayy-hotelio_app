import 'package:flutter/material.dart';

class NearbyHotelPage extends StatelessWidget {
  const NearbyHotelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Nearby Hotel Page'),
      ),
    );
  }
}
